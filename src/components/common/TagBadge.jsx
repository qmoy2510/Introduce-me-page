export default function TagBadge({ label }) {
  return (
    <span className="px-2.5 py-1 text-xs font-medium rounded-full bg-primary text-mint border border-primary/50">
      {label}
    </span>
  )
}
